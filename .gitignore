# Virtual environment
.venv/
venv/

# Downloaded dataset - unzip before use (see README.md)
data/

# Model files (too large for Git)
models/*.h5
models/*.zip

# MediaPipe model (comment out the line below if you want to include it in repo)
# models/hand_landmarker.task

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
dist/
build/